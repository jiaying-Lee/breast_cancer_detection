import numpy as np
#
# a = np.array([1, 2, 3, 4])
# b = np.array((5, 6, 7, 8))
# c = np.array([[11, 2, 8, 4], [4, 52, 6, 17], [2, 8, 9, 100]])


#print(np.argmin(b, axis=0))  # 按每列求出最小值的索引
#print(np.argmin(a, axis=1))  # 按每行求出最小值的索引
# 't' is a tensor of shape [2]
# t = [1,2]
# t1 = np.expand_dims(t, axis=0)
# print(t1)
# t2 = np.expand_dims(t, axis=1)
# print(t2)
# t2 = tf.expand_dims(t, 1)
# print(t2.shape)
# t3 = tf.expand_dims(t, 1)
# print(t3.shape)

#xi = np.expand_dims(X[i], axis=0)

x = [2,-1,1]
w1 = [0.2,]